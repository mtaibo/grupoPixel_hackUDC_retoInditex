services:
  api:
    build: .
    ports:
      - "8001:8001"
    command: uvicorn api:app --host 0.0.0.0 --port 8001
    volumes:
      - .:/app

  web:
    build: .
    ports:
      - "3000:3000"
    command: python3 -m http.server 3000
    volumes:
      - .:/app
